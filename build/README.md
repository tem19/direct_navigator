# Ресурсы сборки

Положите сюда иконку приложения `icon.icns` (1024×1024, формат `.icns`).
На этапе разработки допустима заглушка — она нужна только для `npm run dist`
(electron-builder). `npm run dev` иконку не требует.
