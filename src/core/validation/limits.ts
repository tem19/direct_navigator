/**
 * Лимиты Яндекс Директа для полей сущностей.
 * Значения соответствуют ограничениям API v5 (см. документацию сервисов).
 */
export const DIRECT_LIMITS = {
  campaign: {
    nameMax: 255,
  },
  adgroup: {
    nameMax: 255,
    maxRegions: 100,
  },
  ad: {
    titleMax: 56, // Заголовок 1
    title2Max: 30, // Заголовок 2
    textMax: 81,
  },
  keyword: {
    /** Максимальная длина фразы с учётом минус-слов, символов. */
    phraseMax: 4096,
    /** Максимум слов во фразе (без минус-слов). */
    maxWords: 7,
    /** Минимальная ставка (в валюте аккаунта), для примера — рубли. */
    minBid: 0.3,
  },
} as const;
